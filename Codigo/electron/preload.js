// This file is intentionally minimal. When you need to expose safe APIs
// from main to renderer, use contextBridge here.
// const { contextBridge, ipcRenderer } = require('electron');
// contextBridge.exposeInMainWorld('bridge', { /* safe APIs */ });
